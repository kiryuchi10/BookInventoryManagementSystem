<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE configuration
    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- Type Aliases -->
    <typeAliases>
        <typeAlias type="com.example.demo.model.Book" alias="Book" />
        <!-- Add more type aliases as needed -->
    </typeAliases>

    <!-- Mappers -->
    <mappers>
        <mapper resource="mappers/book.xml" />
        <!-- Add other mapper resources as needed -->
    </mappers>
</configuration>
